# Bare-section cycle 4 Luna review — shard 3

Reviewed all 461 rows from tmp/ina-bare-section-cycle4/review/shard-3.json individually against the full original/before/after fields and built-corpus targets. Widened links, additions/removals, numbered continuations, historical references, mixed INA/USC lists, and authority-tail grammar were checked.

Result: **373 pass; 88 flagged.**

## Flags

- ID 81: The rendered field leaves a mixed INA/USC numeric range (range) after converting only one endpoint. That range is an authority list, so the display is misleading and should retain consistent INA labels or preserve the USC range.
- ID 84: The rendered field leaves a mixed INA/USC numeric range (range) after converting only one endpoint. That range is an authority list, so the display is misleading and should retain consistent INA labels or preserve the USC range.
- ID 87: The rendered field leaves a mixed INA/USC numeric range (range) after converting only one endpoint. That range is an authority list, so the display is misleading and should retain consistent INA labels or preserve the USC range.
- ID 90: The rendered field leaves a mixed INA/USC numeric range (range) after converting only one endpoint. That range is an authority list, so the display is misleading and should retain consistent INA labels or preserve the USC range.
- ID 93: The rendered field leaves a mixed INA/USC numeric range (range) after converting only one endpoint. That range is an authority list, so the display is misleading and should retain consistent INA labels or preserve the USC range.
- ID 96: The rendered field leaves a mixed INA/USC numeric range (range) after converting only one endpoint. That range is an authority list, so the display is misleading and should retain consistent INA labels or preserve the USC range.
- ID 99: The rendered field leaves a mixed INA/USC numeric range (range) after converting only one endpoint. That range is an authority list, so the display is misleading and should retain consistent INA labels or preserve the USC range.
- ID 102: The rendered field leaves a mixed INA/USC numeric range (range) after converting only one endpoint. That range is an authority list, so the display is misleading and should retain consistent INA labels or preserve the USC range.
- ID 105: The rendered field leaves a mixed INA/USC numeric range (range) after converting only one endpoint. That range is an authority list, so the display is misleading and should retain consistent INA labels or preserve the USC range.
- ID 108: The rendered field leaves a mixed INA/USC numeric range (range) after converting only one endpoint. That range is an authority list, so the display is misleading and should retain consistent INA labels or preserve the USC range.
- ID 111: The rendered field leaves a mixed INA/USC numeric range (range) after converting only one endpoint. That range is an authority list, so the display is misleading and should retain consistent INA labels or preserve the USC range.
- ID 114: The rendered field leaves a mixed INA/USC numeric range (range) after converting only one endpoint. That range is an authority list, so the display is misleading and should retain consistent INA labels or preserve the USC range.
- ID 117: The rendered field leaves a mixed INA/USC numeric range (range) after converting only one endpoint. That range is an authority list, so the display is misleading and should retain consistent INA labels or preserve the USC range.
- ID 120: The rendered field leaves a mixed INA/USC numeric range (range) after converting only one endpoint. That range is an authority list, so the display is misleading and should retain consistent INA labels or preserve the USC range.
- ID 123: The rendered field leaves a mixed INA/USC numeric range (range) after converting only one endpoint. That range is an authority list, so the display is misleading and should retain consistent INA labels or preserve the USC range.
- ID 126: The rendered field leaves a mixed INA/USC numeric range (range) after converting only one endpoint. That range is an authority list, so the display is misleading and should retain consistent INA labels or preserve the USC range.
- ID 129: The rendered field leaves a mixed INA/USC numeric range (range) after converting only one endpoint. That range is an authority list, so the display is misleading and should retain consistent INA labels or preserve the USC range.
- ID 132: The rendered field leaves a mixed INA/USC numeric range (range) after converting only one endpoint. That range is an authority list, so the display is misleading and should retain consistent INA labels or preserve the USC range.
- ID 177: The rendered field leaves a mixed INA/USC numeric range (INA 207 to 209) after converting only one endpoint. That range is an authority list, so the display is misleading and should retain consistent INA labels or preserve the USC range.
- ID 180: The rendered field leaves a mixed INA/USC numeric range (INA 207 to 209) after converting only one endpoint. That range is an authority list, so the display is misleading and should retain consistent INA labels or preserve the USC range.
- ID 195: The rendered field leaves a mixed INA/USC numeric range (range) after converting only one endpoint. That range is an authority list, so the display is misleading and should retain consistent INA labels or preserve the USC range.
- ID 198: The rendered field leaves a mixed INA/USC numeric range (range) after converting only one endpoint. That range is an authority list, so the display is misleading and should retain consistent INA labels or preserve the USC range.
- ID 201: The rendered field leaves a mixed INA/USC numeric range (range) after converting only one endpoint. That range is an authority list, so the display is misleading and should retain consistent INA labels or preserve the USC range.
- ID 204: The rendered field leaves a mixed INA/USC numeric range (range) after converting only one endpoint. That range is an authority list, so the display is misleading and should retain consistent INA labels or preserve the USC range.
- ID 207: The rendered field leaves a mixed INA/USC numeric range (range) after converting only one endpoint. That range is an authority list, so the display is misleading and should retain consistent INA labels or preserve the USC range.
- ID 210: The rendered field leaves a mixed INA/USC numeric range (range) after converting only one endpoint. That range is an authority list, so the display is misleading and should retain consistent INA labels or preserve the USC range.
- ID 213: The rendered field leaves a mixed INA/USC numeric range (range) after converting only one endpoint. That range is an authority list, so the display is misleading and should retain consistent INA labels or preserve the USC range.
- ID 216: The rendered field leaves a mixed INA/USC numeric range (range) after converting only one endpoint. That range is an authority list, so the display is misleading and should retain consistent INA labels or preserve the USC range.
- ID 219: The rendered field leaves a mixed INA/USC numeric range (range) after converting only one endpoint. That range is an authority list, so the display is misleading and should retain consistent INA labels or preserve the USC range.
- ID 222: The rendered field leaves a mixed INA/USC numeric range (range) after converting only one endpoint. That range is an authority list, so the display is misleading and should retain consistent INA labels or preserve the USC range.
- ID 225: The rendered field leaves a mixed INA/USC numeric range (range) after converting only one endpoint. That range is an authority list, so the display is misleading and should retain consistent INA labels or preserve the USC range.
- ID 228: The rendered field leaves a mixed INA/USC numeric range (range) after converting only one endpoint. That range is an authority list, so the display is misleading and should retain consistent INA labels or preserve the USC range.
- ID 231: The rendered field leaves a mixed INA/USC numeric range (range) after converting only one endpoint. That range is an authority list, so the display is misleading and should retain consistent INA labels or preserve the USC range.
- ID 234: The rendered field leaves a mixed INA/USC numeric range (range) after converting only one endpoint. That range is an authority list, so the display is misleading and should retain consistent INA labels or preserve the USC range.
- ID 237: The rendered field leaves a mixed INA/USC numeric range (range) after converting only one endpoint. That range is an authority list, so the display is misleading and should retain consistent INA labels or preserve the USC range.
- ID 240: The rendered field leaves a mixed INA/USC numeric range (range) after converting only one endpoint. That range is an authority list, so the display is misleading and should retain consistent INA labels or preserve the USC range.
- ID 243: The rendered field leaves a mixed INA/USC numeric range (range) after converting only one endpoint. That range is an authority list, so the display is misleading and should retain consistent INA labels or preserve the USC range.
- ID 246: The rendered field leaves a mixed INA/USC numeric range (range) after converting only one endpoint. That range is an authority list, so the display is misleading and should retain consistent INA labels or preserve the USC range.
- ID 249: The rendered field leaves a mixed INA/USC numeric range (range) after converting only one endpoint. That range is an authority list, so the display is misleading and should retain consistent INA labels or preserve the USC range.
- ID 252: The rendered field leaves a mixed INA/USC numeric range (range) after converting only one endpoint. That range is an authority list, so the display is misleading and should retain consistent INA labels or preserve the USC range.
- ID 255: The rendered field leaves a mixed INA/USC numeric range (range) after converting only one endpoint. That range is an authority list, so the display is misleading and should retain consistent INA labels or preserve the USC range.
- ID 258: The rendered field leaves a mixed INA/USC numeric range (range) after converting only one endpoint. That range is an authority list, so the display is misleading and should retain consistent INA labels or preserve the USC range.
- ID 261: The rendered field leaves a mixed INA/USC numeric range (range) after converting only one endpoint. That range is an authority list, so the display is misleading and should retain consistent INA labels or preserve the USC range.
- ID 264: The rendered field leaves a mixed INA/USC numeric range (range) after converting only one endpoint. That range is an authority list, so the display is misleading and should retain consistent INA labels or preserve the USC range.
- ID 267: The rendered field leaves a mixed INA/USC numeric range (range) after converting only one endpoint. That range is an authority list, so the display is misleading and should retain consistent INA labels or preserve the USC range.
- ID 270: The rendered field leaves a mixed INA/USC numeric range (range) after converting only one endpoint. That range is an authority list, so the display is misleading and should retain consistent INA labels or preserve the USC range.
- ID 273: The rendered field leaves a mixed INA/USC numeric range (range) after converting only one endpoint. That range is an authority list, so the display is misleading and should retain consistent INA labels or preserve the USC range.
- ID 276: The rendered field leaves a mixed INA/USC numeric range (range) after converting only one endpoint. That range is an authority list, so the display is misleading and should retain consistent INA labels or preserve the USC range.
- ID 279: The rendered field leaves a mixed INA/USC numeric range (range) after converting only one endpoint. That range is an authority list, so the display is misleading and should retain consistent INA labels or preserve the USC range.
- ID 282: The rendered field leaves a mixed INA/USC numeric range (range) after converting only one endpoint. That range is an authority list, so the display is misleading and should retain consistent INA labels or preserve the USC range.
- ID 285: The rendered field leaves a mixed INA/USC numeric range (range) after converting only one endpoint. That range is an authority list, so the display is misleading and should retain consistent INA labels or preserve the USC range.
- ID 288: The rendered field leaves a mixed INA/USC numeric range (range) after converting only one endpoint. That range is an authority list, so the display is misleading and should retain consistent INA labels or preserve the USC range.
- ID 291: The rendered field leaves a mixed INA/USC numeric range (range) after converting only one endpoint. That range is an authority list, so the display is misleading and should retain consistent INA labels or preserve the USC range.
- ID 294: The rendered field leaves a mixed INA/USC numeric range (range) after converting only one endpoint. That range is an authority list, so the display is misleading and should retain consistent INA labels or preserve the USC range.
- ID 297: The rendered field leaves a mixed INA/USC numeric range (range) after converting only one endpoint. That range is an authority list, so the display is misleading and should retain consistent INA labels or preserve the USC range.
- ID 300: The rendered field leaves a mixed INA/USC numeric range (range) after converting only one endpoint. That range is an authority list, so the display is misleading and should retain consistent INA labels or preserve the USC range.
- ID 303: The rendered field leaves a mixed INA/USC numeric range (range) after converting only one endpoint. That range is an authority list, so the display is misleading and should retain consistent INA labels or preserve the USC range.
- ID 306: The rendered field leaves a mixed INA/USC numeric range (range) after converting only one endpoint. That range is an authority list, so the display is misleading and should retain consistent INA labels or preserve the USC range.
- ID 309: The rendered field leaves a mixed INA/USC numeric range (range) after converting only one endpoint. That range is an authority list, so the display is misleading and should retain consistent INA labels or preserve the USC range.
- ID 312: The rendered field leaves a mixed INA/USC numeric range (range) after converting only one endpoint. That range is an authority list, so the display is misleading and should retain consistent INA labels or preserve the USC range.
- ID 384: The rendered field leaves a mixed INA/USC numeric range (INA 412 to 414) after converting only one endpoint. That range is an authority list, so the display is misleading and should retain consistent INA labels or preserve the USC range.
- ID 387: The rendered field leaves a mixed INA/USC numeric range (INA 412 to 414) after converting only one endpoint. That range is an authority list, so the display is misleading and should retain consistent INA labels or preserve the USC range.
- ID 390: The rendered field leaves a mixed INA/USC numeric range (INA 412 to 414) after converting only one endpoint. That range is an authority list, so the display is misleading and should retain consistent INA labels or preserve the USC range.
- ID 393: The rendered field leaves a mixed INA/USC numeric range (INA 412 to 414) after converting only one endpoint. That range is an authority list, so the display is misleading and should retain consistent INA labels or preserve the USC range.
- ID 396: The rendered field leaves a mixed INA/USC numeric range (INA 412 to 414) after converting only one endpoint. That range is an authority list, so the display is misleading and should retain consistent INA labels or preserve the USC range.
- ID 399: The rendered field leaves a mixed INA/USC numeric range (INA 412 to 414) after converting only one endpoint. That range is an authority list, so the display is misleading and should retain consistent INA labels or preserve the USC range.
- ID 402: The rendered field leaves a mixed INA/USC numeric range (INA 412 to 414) after converting only one endpoint. That range is an authority list, so the display is misleading and should retain consistent INA labels or preserve the USC range.
- ID 405: The rendered field leaves a mixed INA/USC numeric range (INA 412 to 414) after converting only one endpoint. That range is an authority list, so the display is misleading and should retain consistent INA labels or preserve the USC range.
- ID 408: The rendered field leaves a mixed INA/USC numeric range (INA 412 to 414) after converting only one endpoint. That range is an authority list, so the display is misleading and should retain consistent INA labels or preserve the USC range.
- ID 411: The rendered field leaves a mixed INA/USC numeric range (INA 412 to 414) after converting only one endpoint. That range is an authority list, so the display is misleading and should retain consistent INA labels or preserve the USC range.
- ID 414: The rendered field leaves a mixed INA/USC numeric range (INA 412 to 414) after converting only one endpoint. That range is an authority list, so the display is misleading and should retain consistent INA labels or preserve the USC range.
- ID 417: The rendered field leaves a mixed INA/USC numeric range (INA 412 to 414) after converting only one endpoint. That range is an authority list, so the display is misleading and should retain consistent INA labels or preserve the USC range.
- ID 420: The rendered field leaves a mixed INA/USC numeric range (INA 412 to 414) after converting only one endpoint. That range is an authority list, so the display is misleading and should retain consistent INA labels or preserve the USC range.
- ID 423: The rendered field leaves a mixed INA/USC numeric range (INA 412 to 414) after converting only one endpoint. That range is an authority list, so the display is misleading and should retain consistent INA labels or preserve the USC range.
- ID 426: The rendered field leaves a mixed INA/USC numeric range (INA 412 to 414) after converting only one endpoint. That range is an authority list, so the display is misleading and should retain consistent INA labels or preserve the USC range.
- ID 462: The rendered field leaves a mixed INA/USC numeric range (INA 411 to 1525) after converting only one endpoint. That range is an authority list, so the display is misleading and should retain consistent INA labels or preserve the USC range.
- ID 465: The rendered field leaves a mixed INA/USC numeric range (INA 411 to 1525) after converting only one endpoint. That range is an authority list, so the display is misleading and should retain consistent INA labels or preserve the USC range.
- ID 468: The rendered field leaves a mixed INA/USC numeric range (INA 411 to 1525) after converting only one endpoint. That range is an authority list, so the display is misleading and should retain consistent INA labels or preserve the USC range.
- ID 498: The rendered field leaves a mixed INA/USC numeric range (INA 245 to 1255) after converting only one endpoint. That range is an authority list, so the display is misleading and should retain consistent INA labels or preserve the USC range.
- ID 501: The rendered field leaves a mixed INA/USC numeric range (INA 245 to 1255) after converting only one endpoint. That range is an authority list, so the display is misleading and should retain consistent INA labels or preserve the USC range.
- ID 504: The rendered field leaves a mixed INA/USC numeric range (INA 245 to 1255) after converting only one endpoint. That range is an authority list, so the display is misleading and should retain consistent INA labels or preserve the USC range.
- ID 507: The rendered field leaves a mixed INA/USC numeric range (INA 245 to 1255) after converting only one endpoint. That range is an authority list, so the display is misleading and should retain consistent INA labels or preserve the USC range.
- ID 510: The rendered field leaves a mixed INA/USC numeric range (INA 245 to 1255) after converting only one endpoint. That range is an authority list, so the display is misleading and should retain consistent INA labels or preserve the USC range.
- ID 513: The rendered field leaves a mixed INA/USC numeric range (INA 245 to 1255) after converting only one endpoint. That range is an authority list, so the display is misleading and should retain consistent INA labels or preserve the USC range.
- ID 516: The rendered field leaves a mixed INA/USC numeric range (INA 245 to 1255) after converting only one endpoint. That range is an authority list, so the display is misleading and should retain consistent INA labels or preserve the USC range.
- ID 519: The rendered field leaves a mixed INA/USC numeric range (INA 245 to 1255) after converting only one endpoint. That range is an authority list, so the display is misleading and should retain consistent INA labels or preserve the USC range.
- ID 522: The rendered field leaves a mixed INA/USC numeric range (INA 245 to 1255) after converting only one endpoint. That range is an authority list, so the display is misleading and should retain consistent INA labels or preserve the USC range.
- ID 525: The rendered field leaves a mixed INA/USC numeric range (INA 245 to 1255) after converting only one endpoint. That range is an authority list, so the display is misleading and should retain consistent INA labels or preserve the USC range.

No residual “section(s) INA” or redundant “of this title/Act” tails remained outside the flagged mixed-range cases. Non-INA Title 5, 18, 22, 28, 32, 37, 42, and 50 references were retained as numeric/official-source citations where the source context required them.
